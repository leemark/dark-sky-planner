// Minimum altitude for Milky Way galactic center to be considered "visible"
export const MW_MIN_ALT_DEG = 15;

// Moon illumination below this threshold = effectively dark (treated as moon-down all night)
export const MOON_INTERFERENCE_THRESHOLD = 0.10;

// Galactic center equatorial coordinates (J2000)
export const GC_RA_HOURS = 17.7611; // 17h 45m 40s
export const GC_DEC_DEG = -29.0078; // -29° 00' 28"

// Sampling interval for MW altitude calculations (minutes)
export const MW_SAMPLE_INTERVAL_MIN = 10;
